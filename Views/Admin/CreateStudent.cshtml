@{
    ViewData["Title"] = "Thêm Học Sinh Mới";
}

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="banner-kids p-4 mb-4" style="background: linear-gradient(135deg, var(--color-primary), var(--color-primary-hover));">
            <h1 class="text-white mb-2"><i class="fa-solid fa-user-plus"></i> Thêm Học Sinh Mới</h1>
            <p class="text-white-50">Tạo tài khoản học sinh đơn lẻ để bắt đầu tham gia làm bài tập và tranh tài.</p>
        </div>

        <div class="card-kids p-4">
            <form method="post" asp-action="CreateStudent" class="form-kids">
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-kids alert-danger-kids alert-dismissible fade show" role="alert">
                        <i class="fa-solid fa-circle-xmark me-2"></i>
                        <strong>Đã có lỗi xảy ra:</strong>
                        <div asp-validation-summary="All" class="mt-2 text-danger small"></div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                <div class="mb-3">
                    <label for="username">Tên đăng nhập (Username) <span class="text-danger">*</span></label>
                    <input type="text" id="username" name="username" class="form-control" placeholder="Ví dụ: nva_lop5a" required />
                    <div class="form-text text-muted small mt-1">
                        <i class="fa-solid fa-circle-info"></i> Hệ thống tự động chuyển thành chữ thường, loại bỏ dấu và ký tự đặc biệt (chỉ giữ lại chữ cái, chữ số và dấu gạch dưới).
                    </div>
                </div>

                <div class="mb-3">
                    <label for="fullName">Họ và tên học sinh <span class="text-danger">*</span></label>
                    <input type="text" id="fullName" name="fullName" class="form-control" placeholder="Ví dụ: Nguyễn Văn An" required />
                </div>

                <div class="mb-4">
                    <label for="password">Mật khẩu truy cập</label>
                    <input type="password" id="password" name="password" class="form-control" placeholder="Nhập mật khẩu (Nếu để trống, hệ thống sẽ tự động tạo ngẫu nhiên)" />
                </div>

                <div class="d-flex gap-2 justify-content-end">
                    <a asp-action="Students" class="btn btn-outline-secondary d-inline-flex align-items-center" style="border-radius: var(--border-radius-md); padding: 10px 24px; font-family: var(--font-heading); font-size: 1.1rem;">
                        Hủy bỏ
                    </a>
                    <button type="submit" class="btn-kids btn-primary-kids m-0">
                        <i class="fa-solid fa-floppy-disk"></i> Tạo Tài Khoản
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
